# Project Rules

## Formatting & Code Style
- Always follow `phpfmt` / PSR-12 formatting rules for all PHP files.
- Format arrays with aligned key-value assignment operators (`=>`).
- Maintain clean, consistent spacing and indentation across all Controllers, Requests, Models, and Services.
